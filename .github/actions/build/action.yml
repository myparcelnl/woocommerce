name: 'Create build'
description: ''

runs:
  using: composite
  steps:
    - name: 'Import translations'
      shell: bash
      #language=sh
      run: |
        yarn translations:import

    - name: 'Build JavaScript bundles'
      shell: bash
      #language=sh
      run: |
        yarn nx run-many \
          --output-style=static \
          --runner=cloud \
          --target=build
