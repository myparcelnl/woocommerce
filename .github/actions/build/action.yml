name: 'Create build'
description: ''

runs:
  using: composite
  steps:
    - name: 'Build JavaScript bundles'
      shell: bash
      #language=sh
      run: |
        yarn nx run-many \
          --output-style=static \
          --runner=cloud \
          --target=build
