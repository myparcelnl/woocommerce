name: '🚀 Release RC'

on:
  push:
    branches:
      - develop

  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/--release.yml
    secrets: inherit

  rebase-prs:
    uses: ./.github/workflows/--rebase-prs.yml
    secrets: inherit
    needs: release
    if: always()
